from math import floor
def main(a, b):
    x=floor(a/b)
    '''find the floor division of a and b and return it.
    
    Args:
        a (int): a number
        b (int): a number
        
    Returns:
        int: the result.
    '''
    return x
print(main(11, 2))